buildscript {
  repositories {
    ecovateRepos()
  }

  dependencies {
    organization: 'com.ecovate', name: 'gradle-avian-plugin', version: '0.1.0'
  }
}

apply plugin: 'avian'
apply plugin: 'proguard'
apply plugin: 'release-manager-and-repos'

repositories {
  ecovateRepos()
}

dependencies {
  avian organization: 'com.ecovate', name: 'avian', version: '0.6.0'
  compile organization: 'org.eclipse', name: 'swt', version: '3.7.2'
}

name = 'pushup-reminder'
organization = 'com.readytalk'
version = '1.0.0'
